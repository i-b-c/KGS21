extends /_templates/layout.pug

block content
    .container-fluid
        .row.page-margin
            section.col-md-12
                .programme.row
                    .col-xs-12.col-md-12
                        h2= self.labels.title.kanut_co_productions
                    .grid-row
                        if self.data.co_productions
                            - var filtered_and_sorted_coprods = self.data.co_productions.filter(e => e.coproduction).sort((a, b) => a.coproduction_importance-b.coproduction_importance)
                            each coprod in filtered_and_sorted_coprods
                                a.col-xs-12.col-sm-4.col-md-3.col-lg-3.co-production(href= `/${coprod.path}` ordinal= coprod.coproduction_importance)
                                    if coprod.X_pictures && coprod.X_pictures.photoMedium
                                        img.img-responsive(src=`https://saal.entu.ee/api2/file-${coprod.X_pictures.photoMedium.split(',')[0]}`)
                                    else
                                        img.img-responsive(src=`/assets/images/placeholder.jpg`)
                                    p.artist= coprod.X_artist
                                    p= coprod[`name_${self.locale}`]
